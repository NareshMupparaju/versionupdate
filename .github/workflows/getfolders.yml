name: List Files Added in Pull Request

on:
  pull_request:

jobs:
  list-modified-folders:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      with:
        ref: ${{ github.event.pull_request.head.ref }}

    - name: Get modified folders
      run: |
        if ! git diff main..[${{ github.event.pull_request.head.ref }}] src/POS.Global; then
            echo "Changes were made!"
          else
            echo "No changes were made."
          fi
          
    - name: Check folder modified
      run: |
        if ! git diff [${{ github.event.pull_request.head.ref }}] -- src/POS.Global; then
            echo "Changes were made!"
          else
            echo "No changes were made."
          fi
          
    - name: Check folder modified HEAD
      run: |
        if ! git diff HEAD -- src/POS.Global; then
            echo "Changes were made!"
          else
            echo "No changes were made."
          fi
